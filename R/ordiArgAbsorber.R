`ordiArgAbsorber` <- function(..., shrink, origin, scaling, triangular,
                              display, choices, const, truemean,
                              length, angle, code, # for arrows
                              FUN)
    match.fun(FUN)(...)
